[build]
  base = ""
  command = "pnpm install --no-frozen-lockfile && pnpm --filter @driver-complaint/shared-types build && pnpm --filter @driver-complaint/admin-web build"
  publish = "apps/admin-web/dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
